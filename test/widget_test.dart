import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // App requires an Isar instance (native plugin) to boot, so widget
    // smoke-testing needs a proper test harness with a fake Isar override.
    // Left intentionally minimal until that harness exists.
  });
}
